{
  "name": "uirouter",
  "version": "1.0.0",
  "description": "Angular UI Router step by step",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Angular UI Router"
  ],
  "author": "webgene",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.8.5"
  },
  "scripts": {
    "postinstall": "bower install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}
